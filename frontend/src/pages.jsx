import { useState } from "react";
function Auth({ signup, goHome, switchMode, onSuccess }) { const [fullName, setFullName] = useState(""); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); async function submit(e) { e.preventDefault(); setError(""); setBusy(true); try { await onSuccess({ username: email, password, full_name: fullName, role: "doctor" }); } catch (err) { setError(err.message); } finally { setBusy(false); } } return <main className="auth-page"><div className="auth-shell"><section className="auth-left"><button className="auth-logo" onClick={goHome}>RETO</button><div className="auth-copy"><p className="auth-eyebrow">RETINAL SCREENING</p><h1>{signup ? "Start with RETO." : "Welcome back."}</h1><p>Secure access for your retinal screening workflow.</p></div></section><section className="auth-right"><div className="auth-form-wrap"><p className="auth-form-eyebrow">{signup ? "NEW ACCOUNT" : "ACCOUNT"}</p><h2>{signup ? "Create account" : "Log in"}</h2><form onSubmit={submit}>{signup && <div className="auth-field"><label>Full name</label><input required value={fullName} onChange={(e) => setFullName(e.target.value)} /></div>}<div className="auth-field"><label>Email address</label><input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} /></div><div className="auth-field"><label>Password</label><input required type="password" minLength="6" value={password} onChange={(e) => setPassword(e.target.value)} /></div>{error && <p className="form-error">{error}</p>}<button className="auth-submit" disabled={busy}>{busy ? "Please wait…" : signup ? "Create account" : "Log in"}<span>→</span></button></form><div className="auth-switch"><span>{signup ? "Already have an account?" : "Don't have an account?"}</span><button onClick={switchMode}>{signup ? "Log in" : "Create account"}</button></div><button className="auth-back" onClick={goHome}>← Back to RETO</button></div></section></div></main>; }
export const Login = (props) => <Auth {...props} signup={false} />;
export const Signup = (props) => <Auth {...props} signup />;
